<nav class="navbar navbar-expand-lg navbar-dark bg-success">

    <a class="navbar-brand" href="{{ path('home') }}">
    {# <img style="width:200px;height:40px" src="logo.PNG" alt="image"> #}
    Aider
    </a>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor03">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="#">Parlons de nous</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">Parrainer</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('donneur_index') }}">Faire un don</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">Ma vie de parrain</a>
            </li>
        </ul>
        <ul class="navbar-nav ml-auto">
            {% if app.user %}
                    <li class="nav-item ">
                       <a href="#" class="nav-link ">
                       
                        <img src="{{app.user.image}}" class="avatar avatar-mini" alt="Avatar de {{app.user.fullName}}"> 
                       </a>
                    </li>
                    <li>    
                    <a href="{{ path('account_logout') }}" class="nav-link">Déconnexion</a>


                       {# <div class="dropdown-menu dropdown-menu-left" aria-labelledby="accountDopdownLink">
                     <a href="{{ path('evenement_new') }}" class="dropdown-item">Créer une annonce</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ path('account_index') }}" class="dropdown-item">Mon compte</a>
                        <a href="{{ path('account_profile') }}" class="dropdown-item">Modifier mon profil</a> 
                        <div class="dropdown-divider"></div>
                        <a href="{{ path('account_logout') }}" class="dropdown-item">Déconnexion</a>≈
                        <a href="{{ path('account_logout') }}" class="dropdown-item">Déconnexion</a>
                    </div>#}
                </li>

            {% else %}
            <li class="nav-item">
                <a href="{{ path("account_register") }}" class="nav-link">Inscription</a>
            </li>
            <li class="nav-item">
                <a href="{{ path('account_login') }}" class="nav-link">Connexion</a>
            </li>
            {% endif %}
        </ul>
    </div>
</nav>

{% for label, messages in app.flashes %}
<div class="container">
    <div class="alert alert-{{ label }}">
        {% for message in messages %}

        <p>{{ message | raw }}</p>

        {% endfor %}
    </div>
</div>
{% endfor %}